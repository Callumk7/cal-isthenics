# Agent Context

## Project purpose

Cal Isthenics is a fresh web app for personalised calisthenics training. The current product surface is a landing page for **FORM**, positioning the app around adaptive programming, workout/progress tracking, consistency, mobility, and long-term strength development.

## Tech stack

- **Runtime/package manager:** Node.js with `pnpm`
- **Framework:** TanStack Start + TanStack Router
- **UI:** React 19, TypeScript, shadcn/ui-style components built with React Aria Components, lucide-react icons
- **Styling:** Tailwind CSS v4 via `@tailwindcss/vite`, global styles in `src/styles.css`
- **Build tooling:** Vite
- **Quality:** ESLint, Prettier, TypeScript, Vitest, Testing Library, jsdom

## Module design and organisation

This is still a template-stage project, so conventions are intentionally light:

- `src/routes/` contains file-based TanStack Router routes.
  - `src/routes/__root.tsx` defines the root HTML shell, metadata, styles, scripts, and not-found UI.
  - `src/routes/index.tsx` currently contains the landing page and local presentation data.
- `src/router.tsx` creates and types the TanStack Router instance.
- `src/routeTree.gen.ts` is generated by TanStack Router; do not edit manually.
- `src/components/ui/` contains reusable UI primitives/components. Interactive primitives should use `react-aria-components`.
- `src/hooks/` contains shared React hooks.
- `src/lib/` contains shared utilities such as `cn()` for class merging.
- `public/` contains static assets and web metadata.

As features grow, prefer keeping route-level composition in `src/routes/`, reusable product components outside routes, and domain/business logic in clearly named modules under `src/`.

## Testing

Vitest + jsdom is configured via `vitest.config.ts` (standalone — does not run the TanStack Start Vite plugin). The setup file is `src/test/setup.ts`, which imports `@testing-library/jest-dom/vitest` matchers and runs `cleanup()` after each test.

Test files are colocated with the code they test and follow the `*.test.ts` / `*.test.tsx` naming convention (e.g. `src/lib/utils.test.ts`). Imports from `vitest` are explicit — globals are off.

Scripts:

- `pnpm test` — run all tests once (used in CI)
- `pnpm test:watch` — run Vitest in watch mode during development
- `pnpm typecheck` — run TypeScript without emitting files
- `pnpm lint` — run ESLint
- `pnpm check` — check formatting with Prettier

CI (`.github/workflows/ci.yml`) runs `pnpm test`, `pnpm typecheck`, and `pnpm lint` on every push to `main` and on pull requests.

## Deployment

Deployment is not defined yet. The project currently builds with:

```bash
pnpm build
```

Until a target platform is chosen, treat deployment as TBD and avoid adding platform-specific assumptions. Likely future work is to select a TanStack Start-compatible deployment target, document required environment variables, and add any adapter/configuration needed for that target.

## Useful commands

```bash
pnpm dev       # start local dev server on port 3000
pnpm build     # production build
pnpm preview   # preview built app
pnpm test      # run tests
pnpm lint      # lint
pnpm typecheck # TypeScript check
pnpm check     # formatting check
```
